node_modules/
.env
.env.local
.env.*.local
.env.testnet
.env.mainnet
build/
build-withdraw/
build-compliance/
dist/
.next/
target/
*.tsbuildinfo

# Circuit artifacts (large binaries)
*.ptau
*.zkey
*.wasm
*.r1cs
*.wtns
*.sym

# Sui
contracts/Published.toml

# withdraw_vk.json is a dev-only single-contributor-ceremony artifact (new random entropy
# every `compile-withdraw.sh` run) — unlike transfer_vk.json/compliance_vk.json, it was never
# committed on purpose. Committing whatever this session's ceremony happened to produce would
# ship a VK inconsistent with the deployed verifier. See docs/research/EXPERIMENTS.md #13.
frontend/public/circuits/withdraw_vk.json
