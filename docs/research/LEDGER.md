# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-14 | `ci-ptau-blocker-and-constraint-attribution` | (1) The `circuit-tests` CI job's Powers-of-Tau download returns 403 on GitHub Actions and is fixable by generating it locally. (2) The depth-20 Poseidon Merkle-membership path, not the fixed-arity identity/nullifier/credential hashes, accounts for >50% of `transfer.circom`'s and `compliance.circom`'s non-linear constraints. | **KEEP** | This is the actual reason `main` has had exactly one ledger row since 2026-07-22: found **30 open, unmerged `research:` PRs** (#32-#61, 2026-08-14 through 2026-09-13) on GitHub, most re-doing the same Poseidon2-vs-Poseidon question because every prior night's PR silently failed to merge. Root cause confirmed directly in CI logs: `curl` to `storage.googleapis.com/zkevm/ptau/...` returns `403` on GitHub Actions runners, failing the `circuit-tests` job before any test runs, on every PR. Fixed `.github/workflows/ci.yml` and `circuits/scripts/compile.sh` to generate the ptau locally instead (`snarkjs powersoftau new/contribute/prepare phase2`, no network) and validated end-to-end in this session: real zkeys generated for all three circuits, 108/108 circuit tests passing in full-proof (not fallback) mode. Separately built an exact constraint-attribution methodology (`circuits/bench/components/`) that reproduces `BASELINE.md`'s totals precisely and explains why PR #61's Poseidon2 REJECT was correct (the swap only ever touched 18-24% of the relevant budget). **Also independently re-confirmed a Critical, still-unfixed vulnerability** (`zk_withdraw` never checks `recipientHash` against the caller-supplied `recipient` — front-running/fund-theft, first found 2026-09-11 PR #59, documented as threat-model RR10 in unmerged PR #60) and pushed a real-time notification about it — not fixed in this PR, needs dedicated review. Also found `compliance-utils.ts`'s `buildMerkleTree` costs 64.2s (measured) for a single-leaf depth-20 tree (O(2^depth), no zero-subtree caching) — queued, not fixed tonight. Full writeup: [`2026-09-14-ci-ptau-blocker-and-constraint-attribution.md`](2026-09-14-ci-ptau-blocker-and-constraint-attribution.md). |
