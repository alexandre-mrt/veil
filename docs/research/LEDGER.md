# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-07-30 | `poseidon2-merkle-hash` | Swapping circomlib's `Poseidon(2)` for a Poseidon2 permutation in the depth-20 Merkle-path hasher (`templates/merkle_proof.circom`, the dominant Poseidon call site in `transfer.circom`/`compliance.circom`) reduces R1CS constraint count and Node proving time for both circuits. | **REJECT** | Falsified: the vendored `@taceo/circom-lib` t=3 Poseidon2 template *increases* R1CS constraints by +1,260 (+9.3% transfer, +9.9% compliance) — its linear layer uses named-signal intermediates instead of circomlib's `var`-accumulator folding, costing one extra R1CS row per intermediate. Real proving time moved only +1.2–1.5% (inconclusive vs. measurement noise) because both circuit sizes round up to the same 16,384-constraint FFT domain either way. Correctness of the underlying permutation was independently verified before use: round constants diffed byte-for-byte against the canonical HorizenLabs reference (all 64 rounds matched) and 8 end-to-end test vectors (incl. field-boundary cases) matched the independent `@taceo/poseidon2` TS reference. 8 new negative/positive tests added (`circuits/test/poseidon2-merkle.test.mjs`), including two that prove the swap is actually enforced (an old-Poseidon-computed root is rejected by the new circuit). Full suite green (unrelated `sui move test` still blocked, no contract touched). Queue item #1 (on-chain gas) re-attempted and blocked again, this time by a hard org-policy `403` on both `fullnode.testnet.sui.io` and `api.github.com` — a third distinct blocker across three nights. Full writeup: [`2026-07-30-poseidon2-merkle-hash.md`](2026-07-30-poseidon2-merkle-hash.md). |
