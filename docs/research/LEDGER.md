# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-24 | `recipient-binding-fix` | `pool::zk_withdraw` can be made to reject a recipient-substitution attack (real, unpatched fund-theft bug — see notes) by recomputing `recipientHash` on-chain via `sui::poseidon`, with zero circuit change and zero VK rotation. | **KEEP** | Before starting, audited the 10-PR open backlog (`#64`-`#73` since 2026-07-28, mostly duplicate Poseidon2/gas reruns caused by two long-unfixed CI infra bugs) and found `#71` (2026-09-21, still unmerged) had already documented a **critical, unpatched vulnerability**: `pool::zk_withdraw` never checked the proof's committed `recipientHash` against the caller-supplied `recipient` argument, so anyone observing a pending withdrawal's proof could resubmit it with their own address and steal the funds. Re-verified the bug directly against `main` (not just trusted the unmerged PR's claim), then fixed it: `pool.move`/`verifier.move` now recompute `Poseidon(8, recipient)` on-chain via `sui::poseidon::poseidon_bn254` and assert it matches. Verified that precompile matches `circomlib`'s Poseidon bit-for-bit (4/4 reference vectors, real `sui move test` run against a `sui` CLI binary version-matched to `Move.toml`'s pinned framework rev) before trusting the approach. Regression test uses a REAL Groth16 proof (circom 2.2.2 + local trusted setup, independently `snarkjs`-verified) reproducing the exact attack and confirming it now aborts. Full Move suite: 124 to 127 pass. `docs/threat-model.md` E7 corrected from a false "Mitigated". No `BASELINE.md` change (not a performance number). Flagged the PR backlog itself to the repo owner as the real top priority — a scheduled run shouldn't merge/close PRs unilaterally. Full writeup: [`2026-09-24-recipient-binding-fix.md`](2026-09-24-recipient-binding-fix.md). |
