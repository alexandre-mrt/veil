# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-16 | `poseidon2-microbench` | Swapping Poseidon for Poseidon2 at Veil's actual arities (2-input Merkle hash, 3-input tx/nullifier hashes) measurably reduces R1CS constraint count and Groth16 proving time. | **REJECT** | Spent early part of the run re-attempting queue item #1 (on-chain gas) first, per the 2026-07-22 note — confirmed BLOCKED more thoroughly this time (no `sui` on crates.io, GitHub access scoped to this repo only so no release download, every Sui RPC host categorically rejected by egress policy) and left it blocked rather than re-running an unchanged result; see `EXPERIMENTS.md` item #1 for the updated notes. Main experiment: built an isolated (non-shipped) circom Poseidon2 implementation (`circuits/bench/`) since no such library exists on npm, cross-checked its round constants bit-for-bit between two independent npm packages, verified circuit output against a native reference, and added a negative test proving a malicious witness is rejected. Measured constraint count (essentially a wash: -0.4% at t=3, +1.2% at t=4) and Groth16 proving time (-28.7% at a single 2-input hash, but only -4.9% at realistic depth-20-Merkle-proof scale, because Poseidon2's saving is in native witness-generation arithmetic, not the constraint count that actually drives Groth16 cost). Also found t=5/t=6 (half of Veil's Poseidon calls) have no standard Poseidon2 parameterization at all. REJECTed: no gas benefit, ~a wash on constraints, a small (~3-4% projected whole-circuit) proving-time win not worth a new trusted-setup ceremony + audit + running two hash functions. Full writeup: [`2026-09-16-poseidon2-microbench.md`](2026-09-16-poseidon2-microbench.md). |
