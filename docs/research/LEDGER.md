# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-07 | `poseidon-constraint-decomposition` | Every R1CS constraint in all three circuits can be attributed exactly to an isolated circomlib gadget instance (Poseidon per arity, the depth-20 Merkle path, `Num2Bits`, comparators), measured independently. | **KEEP** | Not a Poseidon2 swap — the prerequisite measurement for one. Isolated 11 gadget instances behind standalone `component main` probes (`scripts/bench/constraint-probes/`), summed them, and matched all three circuits' constraint totals **exactly** (down to the constraint, with every glue residual identified). Finding: Poseidon is 89–98% of every circuit's constraint budget; the depth-20 Merkle path alone is 76–82% of `transfer.circom`/`compliance.circom` — far more than the other Poseidon instances combined. `BASELINE.md` updated with the decomposition table. Also re-verified item #1 (on-chain gas) is still **BLOCKED**, now with clean evidence on both routes (GitHub scoped to this repo only; `fullnode.testnet.sui.io` denied by egress policy) instead of last time's ambiguous denial. New blocker found: the ptau URL `compile*.sh` depends on now returns `403` from GCS itself, so circuit tests ran in fallback (non-full-proof) mode tonight — 108/108 circuit + 109/109 converter + 67/67 compliance-utils + 19/19 frontend + 6/6 fuzz-property tests pass; Move suite still blocked (no `sui` CLI). Full writeup: [`2026-09-07-poseidon-constraint-decomposition.md`](2026-09-07-poseidon-constraint-decomposition.md). |
