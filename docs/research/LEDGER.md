# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-12 | `onchain-gas-baseline` | Every Veil entry point's real Sui gas cost can be measured from actual transaction effects on a real Move VM, closing `BASELINE.md`'s last blank axis. | **KEEP** | Closes queue item #1, blocked twice before. Direct JSON-RPC to testnet/mainnet fullnodes confirmed genuinely blocked at the network-policy layer this time (proxy status endpoint shows explicit `connect_rejected` / policy denial, not a transient failure — ruled out for good). Unblocked instead by building `sui` 1.78.0 and `circom` 2.2.2 from source (git clone over HTTPS works even though github.com's web/API returns 400/403) and running a local single-validator network (`sui start`) — real Move VM, real gas metering, zero outbound calls, though not the specific already-deployed testnet package. All 11 distinct entry points measured with real gas from `effects.gasUsed`, including `compliance::compliant_transfer`'s dual Groth16 verify. Unexpected finding: computation cost is bucketed and identical (1,000,000 MIST) across every entry point measured — storage cost is what actually determines net gas, which reframes queue item #3's batching hypothesis (re-ranked below). Full Move test suite (124) and every other suite re-run and green. Full writeup: [`2026-08-12-onchain-gas-baseline.md`](2026-08-12-onchain-gas-baseline.md). |
