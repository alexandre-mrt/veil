# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-01 | `poseidon2-constraint-delta` | Swapping Veil's Poseidon instances for Poseidon2 reduces non-linear constraint count and Node proving time for all three circuits. | **REJECT** (naive full swap) | On-chain gas (queue #1) re-attempted first: confirmed `crates.io`'s `sui`/`sui-sdk` are unrelated name-squatted placeholder crates (0 deps, versions 0.0.1/0.0.0), no usable CLI installable — **BLOCKED** a third time, re-ranked down (see `EXPERIMENTS.md`). Moved to #2: built full, real, compiling Poseidon2 variants of all three circuits (`circuits/poseidon2-experiment/`, `@taceo/circom-lib` 0.9.0 + `@taceo/poseidon2` 0.2.0 JS witness generation) and measured real Groth16 setup + proving time on this machine. Result: `transfer` +31.8% Node proving time, `withdraw` +27.0%, `compliance` -8.5% — a net regression, caused by the library's supported permutation widths (`t∈{2,3,4,8,12,16}`) not covering two of Veil's four real arities (`t=5,6`), forcing a zero-pad to `t=8` that crosses a Groth16 FFT domain-size doubling for `transfer`/`withdraw`. Real negative test confirms the experimental gadget is soundly constrained (not the cause of the regression). `BASELINE.md` gets one addition despite the REJECT: a per-instance Poseidon constraint breakdown (answers 2026-07-22's open question #4) — Poseidon accounts for 93-94% of `transfer`/`compliance`'s non-linear constraints and 78% of `withdraw`'s, with the depth-20 Merkle path alone at 75-80%. Full writeup: [`2026-09-01-poseidon2-constraint-delta.md`](2026-09-01-poseidon2-constraint-delta.md). |
