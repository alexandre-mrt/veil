# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-10 | `poseidon2-merkle` | Swapping Poseidon (circomlib) for Poseidon2 (`@taceo/circom-lib`) reduces R1CS constraint count and Groth16 proving time at the arities Veil's circuits actually use (Merkle t=3, tx-amount t=4, commitment/nullifier t=5-padded-to-8). | **REJECT** (for this library as a drop-in) | Re-attempted item #1 (gas) first — confirmed still **BLOCKED**: `fullnode.testnet.sui.io` RPC (403, egress policy) and, newly checked, `static.crates.io` crate downloads (403, same policy) both closed, so `sui` CLI is unreachable by either RPC or from-source build. Pivoted to item #2. Measured real constraint counts and proving time for Poseidon vs Poseidon2 at all three arities Veil uses, isolated and in a full `transfer_poseidon2.circom` prototype (not wired into production): constraints got **worse** at every arity (+12.1% Merkle, +40.8% t=4, +126.0% t=8-padded t=5; +11.1% for the full prototype circuit), but proving time did *not* regress (−2.7% to −3.4%, Groth16's power-of-2 FFT domain absorbed the extra constraints). Root cause identified: the library's "efficient" linear layer materializes extra R1CS rows that circomlib's classic `Mix()` folds away for free — a fixable implementation gap, not a fundamental Poseidon2 limit. 5 negative tests (real Groth16 proofs) confirm a malicious witness is still rejected. Full test suite green (108 circuit + 5 negative + 109 converter + 67 compliance-utils + 6×500 fuzz + 19 frontend; Move tests still not run, same blocker as item #1). `BASELINE.md` unchanged — nothing shipped to production. Full writeup: [`2026-08-10-poseidon2-merkle.md`](2026-08-10-poseidon2-merkle.md). |
