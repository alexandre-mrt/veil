# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-04 | `poseidon2-constraints` | Swapping circomlib Poseidon for Poseidon2 (SAFE-style, tag in the sponge capacity) in Veil's exact hash shapes reduces R1CS constraints and Groth16 proving time for `transfer`/`compliance`/`withdraw`. | **PARK** | Re-attempted queue item #1 (on-chain gas) first per the queue's own note; re-confirmed **BLOCKED** with harder evidence this time (direct `curl` to `fullnode.testnet.sui.io` and a control domain both hard-blocked by a confirmed org egress allowlist, not a retryable denial — see the report). Fell through to item #2. Built 6 hash-only bench circuits (3 production shapes × {current, poseidon2}) under `circuits/bench/`, using TACEO's audited `@taceo/circom-lib`/`@taceo/poseidon2` (no round constants hand-derived). Real, measured result, half-confirms the hypothesis: **total R1CS constraints rose 11.5-16.4%** (linear-constraint growth from the sponge's matrix-multiply decomposition, plus a forced width jump for the one shape needing 4 non-tag values, since Poseidon2 has no t=5/6 parameters) but **Groth16 proving time dropped 4.4-14.9%** across all three shapes (clean 10-run measurements) — non-linear constraints, which fell in every shape, tracked real proving time far better than the total-constraint number did. Malicious-witness rejection: 11/11. Not KEEP because no production circuit was touched (a real swap breaks already-issued testnet commitments — out of scope for one night) and the domain-tag derivation used is a simplified stand-in for the full SAFE spec. `circom`/ptau were also unreachable this session (same egress block as item #1) — validated `circom2` (WASM) against `BASELINE.md`'s exact numbers before trusting it. Full writeup: [`2026-09-04-poseidon2-constraints.md`](2026-09-04-poseidon2-constraints.md). |
