# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-07 | `poseidon-constraint-attribution` | Each circuit's R1CS non-linear constraint count can be decomposed into named gadget contributions (Poseidon per arity, `Num2Bits`, comparators, the depth-20 Merkle path), accounting for ≥99% of the real compiled total, without touching any production circuit. | **KEEP** | Re-attempted on-chain gas (queue #1) first: network is no longer the blocker (`git`/`cargo` reach `MystenLabs/sui` fine), but a full `sui` CLI build is compute-bound on this sandbox's 4 vCPUs — a multi-hour job, still mid-compile after 15 min when re-parked; a background attempt was lost to an unrelated container restart and not resumed, to keep this a one-hypothesis night. Fell back to queue #2's alternate framing (constraint attribution instead of a literal Poseidon2 port — see report for why: a from-scratch Poseidon2 circom implementation is real-crypto risk, and an unverified literature figure suggests Poseidon2's win is Plonk-specific, not R1CS/Groth16). Attribution hit 99.97–99.99% coverage on all three circuits; the depth-20 Merkle path alone is 75–80% of non-linear constraints in both circuits that carry one — bigger than every other Poseidon call combined. `BASELINE.md` gained a new attribution section; `EXPERIMENTS.md` re-ranked (item 4, Merkle-depth scaling, now ranks above item 9, Poseidon2/proof-system swap). Full writeup: [`2026-08-07-poseidon-constraint-attribution.md`](2026-08-07-poseidon-constraint-attribution.md). |
