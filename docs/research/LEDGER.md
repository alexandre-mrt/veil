# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-23 | `poseidon-constraint-cost` | Poseidon hashing (incl. the depth-20 Merkle check) accounts for the large majority of non-linear constraints in `transfer.circom`/`compliance.circom`, and non-linear constraint count is exactly additive across gadgets under circom's default `--O1`. | **KEEP** | Queue item #2, taken in its explicitly-permitted alternate form (gadget-cost breakdown, not a Poseidon2 port — no verified circom Poseidon2 implementation could be sourced tonight; `api.github.com` code search is `403`-blocked by org policy and guessing a crypto gadget's source URL is a supply-chain risk not worth taking). New reusable tool: `scripts/bench/poseidon-cost/`. Result: predicted (sum of standalone gadget costs) matches actual exactly for all three circuits (0 delta); Poseidon is 94.0% of `transfer.circom`, 95.3% of `compliance.circom`, 78.0% of `withdraw.circom`'s non-linear constraints. Bonus measurement for queue item #4: Merkle-depth cost is exactly linear, 246.0 non-linear constraints/level. Queue item #1 (on-chain gas) re-checked and still **BLOCKED** — now with a firmer signal (explicit egress-proxy `403` on the `fullnode.testnet.sui.io` RPC fallback, organization policy, not retried). Full writeup: [`2026-09-23-poseidon-constraint-cost.md`](2026-09-23-poseidon-constraint-cost.md). |
