# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-18 | `poseidon2-vs-poseidon` | Swapping the 20-level Merkle-membership node hash in `transfer.circom`/`compliance.circom` from circomlib `Poseidon(2)` (sponge) to `Poseidon2(2)` (compression mode) measurably reduces R1CS constraints and, more weakly, Node proving time. | **KEEP** (experimental — not deployed) | On-chain gas (queue #1) re-attempted first, re-confirmed **BLOCKED** for a new, specific reason this time: `github.com` and `fullnode.{testnet,devnet}.sui.io` both denied by the session's egress policy (403), `api.github.com` reachable but scoped to `alexandre-mrt/veil` only, `static.crates.io` denied (403). Moved to queue #2. Built `transfer_poseidon2.circom`/`compliance_poseidon2.circom` (parallel to, not replacing, the deployed circuits) using `@taceo/circom-lib`/`@taceo/poseidon2` (independently KAT-verified at Merkle depths 1–20, which caught a real wrong-field-modulus bug in my own JS glue code before it reached the benchmark). Measured: −4.85%/−5.18% R1CS constraints, −4.31%/−4.55% zkey size, −4.6%/−5.6% Node proving time (transfer/compliance respectively, 20-run means). 9 new real-Groth16 tests including 6 malicious-witness rejection cases; all 108 existing circuit tests + 67 compliance-utils + 6 fuzz properties + 109 converter + 19 frontend tests still pass unchanged. `sui move test` still **NOT RUN** (same CLI blocker). `BASELINE.md` gets a new, clearly-separate "Poseidon2 candidate" section — deployed-circuit rows untouched, since adopting this needs its own production trusted-setup ceremony and pool migration, not attempted tonight. Full writeup: [`2026-08-18-poseidon2-vs-poseidon.md`](2026-08-18-poseidon2-vs-poseidon.md). |
