# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-18 | `onchain-gas-baseline` | Real on-chain gas per Move entry point (`deposit`, `shielded_transfer`, `zk_withdraw`, `compliant_transfer`, admin ops) can be measured from actual executed transactions, closing `BASELINE.md`'s one BLOCKED axis. | **KEEP** | Unblocked the `sui` CLI via a GitHub *Releases* download (raw API/RPC hosts stayed blocked, but that path worked — a different finding than 2026-07-22's identical-looking blocker) and matched the CLI's release tag to `Move.toml`'s pinned framework `rev`. Ran everything on a local network (`sui start --force-regenesis`) since `fullnode.testnet.sui.io` is still blocked; same protocol binary, same gas schedule. Measured 13 entry points end-to-end, including 3 calls that verify real Groth16 proofs on-chain (`shielded_transfer`, `zk_withdraw`, `compliant_transfer` with 2 proofs in one call). Also ran the Move test suite for the first time (124/124 pass) — same toolchain unblock. Key finding: Sui's computation-gas bucketing makes Groth16 verification compute-cost-free; all gas differences are storage-driven. `BASELINE.md` updated in place. Full writeup: [`2026-09-18-onchain-gas-baseline.md`](2026-09-18-onchain-gas-baseline.md). |
