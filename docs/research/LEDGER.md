# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-21 | `backlog-audit-and-recipient-binding-vuln` | N/A — process audit + security reconfirmation, not a moved number (see report). | **KEEP** | Every night since 2026-07-22 correctly picked the top queue item, did real work, and opened a PR — but nothing merged, so `main`'s queue/ledger never advanced and **27 open PRs** (#41-#70) piled up, ≥16 independently re-deriving the Poseidon2-vs-Poseidon REJECT and ≥6 independently re-deriving the on-chain-gas BLOCKED→closed result, plus ≥5 independent fixes for the same 2 recurring CI infra bugs (a broken `oven-sh/setup-bun` SHA pin, and `storage.googleapis.com`'s ptau host now 403ing GitHub-hosted runners). **PR #68 is fully green (CI success) and `mergeable_state: clean`** — recommend merging it (closes queue item #1 for real) and closing the superseded duplicates. Separately, independently re-confirmed (direct code read of `contracts/sources/pool.move:571-630`, current `main`, not taken from an unmerged PR's claim) that **`docs/threat-model.md` E7 is false**: `zk_withdraw` never checks `recipientHash` against the caller-supplied `recipient`, so anyone can redirect a pending withdrawal's payout to their own address. E7 corrected to "Not mitigated", new **RR10 (Critical)** added. Not fixed here (needs a circuit change + VK rotation + soundness argument + negative test — out of scope for one unattended night); filed as `EXPERIMENTS.md` #1. Also ran this session's own from-scratch Poseidon2 constraint-count microbenchmark (`circuits/research/poseidon-bench/`, `scripts/bench/poseidon-arity.mjs`) before finding the backlog — corroborates the existing unanimous REJECT (0.00% non-linear constraint delta for the depth-20 Merkle hasher) — but does **not** open a 19th duplicate PR for it; folded into this report instead. Full writeup: [`2026-09-21-backlog-audit-and-recipient-binding-vuln.md`](2026-09-21-backlog-audit-and-recipient-binding-vuln.md). |
