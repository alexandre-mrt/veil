# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-24 | `poseidon2-merkle-hasher` | Swapping circomlib's `Poseidon(2)` for Poseidon2(t=3) in `templates/merkle_proof.circom` (the depth-20 node hash, 20 calls/proof in both `transfer.circom` and `compliance.circom`) measurably reduces constraint count and proving time. | **REJECT** | Disproven by measurement, not left unconfirmed. Non-linear constraints drop slightly (~1%) as predicted, but TACEO's `@taceo/circom-lib` linear-layer encoding (named-intermediate-signal MDS multiplies) costs far more in linear R1CS constraints: transfer/compliance +1,260 constraints (+9.3%/+9.9%), withdraw +63 (+2.1%) — exactly +63/Poseidon2 call site, both circuits. Real proving-time regression measured in both Node (+14–19%) and headless Chromium (+6–11%) across all three circuits; zkey +1.4–7.0% larger. Cross-checked the circom template against `@taceo/poseidon2`'s JS implementation first (byte-identical output for a fixed input) before trusting it in-circuit. 109/109 circuit tests pass (real Groth16 fullProve+verify), including a new negative test (T44) proving a Merkle root computed with the old Poseidon(2) is rejected by the new circuit. `main`/`BASELINE.md`/`docs/threat-model.md` unchanged — branch kept for the record. On-chain gas (queue item 1) blocked a third time, now by network-proxy policy denial to the testnet fullnode rather than a CLI/tooling gap; downgraded in the queue re-rank pending a materially different angle. Full writeup: [`2026-08-24-poseidon2-merkle-hasher.md`](2026-08-24-poseidon2-merkle-hasher.md). |
