# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-28 | `poseidon2-merkle-hash` | Swapping `transfer.circom`/`compliance.circom`'s depth-20 Merkle-node hash from circomlib Poseidon to a Poseidon2 compression (`@taceo/circom-lib`, audited t=2 parameters) measurably cuts R1CS constraints and Node proving time, with the swap kept out of production circuits pending a migration plan. | **PARK** | On-chain gas (queue item 1) blocked a third time, for a third distinct reason: the proxy gateway itself returns 403 for both `fullnode.testnet.sui.io` and `github.com` (confirmed via `/__agentproxy/status`, not a tool-approval prompt this time) — no `sui` CLI reachable or buildable within budget. Moved to queue item 2 (Poseidon2) instead. Measured a real, isolated Merkle-node-hash swap: −660 R1CS constraints (−4.85% `transfer.circom`, −5.18% `compliance.circom`, −6.35% in isolation), −7.1% mean Node proving time (n=20) on `transfer.circom`, cross-checked against an independent JS reference implementation and confirmed to reject 4 classes of malicious witness. Kept as experimental circuits under `circuits/experiments/` (not wired into `contracts/`, `scripts/src/`, or `frontend/`) because a real production swap needs off-chain tree-builder migration (`frontend/src/lib/merkle-tree.ts`, `scripts/src/compliance-utils.ts`), a new timelocked VK + ceremony contribution, and a migration path for already-deposited commitments — none of which fit in one night. The majority-weight domain-tagged hashes (`Poseidon(4)`/`Poseidon(5)`, commitment/nullifier/credential-leaf) are untouched: no audited Poseidon2 parameters exist at those widths. Full writeup: [`2026-08-28-poseidon2-merkle-hash.md`](2026-08-28-poseidon2-merkle-hash.md). |
