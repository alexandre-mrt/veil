# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-17 | `poseidon2-merkle-compression` | Swapping the depth-20 Merkle-membership hash from circomlib Poseidon (sponge) to Poseidon2 (2-to-1 compression mode, `@taceo/circom-lib`'s own audited construction) reduces `transfer.circom`'s R1CS constraint count and Groth16 proving time. | **KEEP** (research artifacts + a real, positive, reproducible finding — not adopted into the production circuits) | Full non-linear-constraint attribution across all three circuits (exact match to the 2026-07-22 baseline totals) found the Merkle tree is 75-80% of non-linear cost, not the identity/credential hashes as previously guessed. A naive sponge-mode Poseidon2 swap made things *worse* (+12-41% constraints); the audited library's own compression-mode construction made things better: -4.9% constraints, -4.3% zkey size, -5.9% proving time (844.35ms→794.15ms), measured end-to-end on a real `transfer_poseidon2.circom` variant with 3 passing adversarial-witness tests. Not adopted: Poseidon2 has no official parameters for the t=5/t=6 states the identity/credential hash sites need (architecturally partial swap), and shipping even the partial swap needs a VK-rotation ceremony out of scope for one night. `BASELINE.md` gets a new "Research candidates" line, not a changed protocol-state row. On-chain gas (queue item #1) re-attempted and **still BLOCKED** — narrower diagnosis this time (network policy denies `github.com` releases, `storage.googleapis.com`, and direct Sui RPC alike). Full writeup: [`2026-09-17-poseidon2-merkle-compression.md`](2026-09-17-poseidon2-merkle-compression.md). |
