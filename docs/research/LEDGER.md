# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-22 | `poseidon-constraint-attribution` | Every gadget's non-linear-constraint cost in the three production circuits can be measured in isolation and summed back to each circuit's whole-circuit total from `BASELINE.md` to within a handful of constraints. | **KEEP** | Fallback path for queue item #2 (a real Poseidon2 swap was rejected — no vetted circom implementation exists, and self-deriving round constants without a reference to check against is a soundness risk, not an optimization). Built 12 isolated gadget circuits (`circuits/bench-primitives/`) + two reusable scripts (`circuits/scripts/bench-primitives.sh`, `scripts/bench/poseidon-attribution.mjs`, the latter asserting the reconciliation so it fails if a future circuit edit invalidates it). Reconciliation is exact: 0 non-linear residual on all three circuits. Poseidon-attributable share of non-linear constraints: `transfer.circom` 93.1%, `compliance.circom` 94.3%, `withdraw.circom` 78.0%. Merkle-path cost confirmed exactly linear in depth (520 constraints/level). `BASELINE.md` updated with a new "Constraint attribution" section. On-chain gas (queue item #1) was re-attempted as a bounded background side task tonight — `git clone`/`cargo build` of the `sui` CLI from source is no longer blocked the way it was on 2026-07-22 (that night's `github.com`/`crates.io` denials do not reproduce), but the build did not finish compiling within tonight's window; still BLOCKED, but on a build-time budget now, not a toolchain-access wall. Full writeup: [`2026-09-22-poseidon-constraint-attribution.md`](2026-09-22-poseidon-constraint-attribution.md). |
