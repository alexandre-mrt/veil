# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-06 | `ci-backlog-audit` | Diagnostic, not a performance hypothesis: why do items #1/#2 still show as unsettled after ~40 nights that each claim to have run them? | **KEEP** (CI fix) / **PARK** (the rest, needs a maintainer decision) | Not a crypto/scalability night. Found **35 open, unmerged research PRs** (#18–#54, 2026-07-29 to 2026-09-05) — this ledger and `EXPERIMENTS.md` on `main` are still frozen at the 2026-07-22 row because nothing has merged since, so every night has been re-running the same top-of-queue items blind to prior nights' (unmerged) results: independently duplicated Poseidon2-vs-Poseidon REJECTs (6+ nights) and on-chain-gas BLOCKED findings (3+ nights). Root cause: CI is red on every PR — `oven-sh/setup-bun` is pinned to a SHA that doesn't exist in that repo (fails in ~2s, before any tests run), and `move-tests`/`circuit-tests` fail on `403`s downloading the `sui` CLI release and the pot15 `.ptau` file, which reads as an Actions-runner network allowlist boundary, not a code bug. The bun-pin fix was already correctly diagnosed once before (commit `4dc78ea`, 2026-08-31) but that fix itself sat on an unmerged branch. Re-applied it here (verified still broken on `main` beforehand). The network-block half is **PARK** — flagged for the maintainer rather than routed around. Full writeup: [`2026-09-06-ci-backlog-audit.md`](2026-09-06-ci-backlog-audit.md). |
