# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-20 | `poseidon-constraint-attribution` | Every non-linear R1CS constraint in the three circuits can be attributed to a specific gadget instance by compiling it alone, and the resulting per-gadget sums reproduce `BASELINE.md`'s per-circuit totals exactly. | **REJECT** (original Poseidon2 framing) / measurement **KEEP** | Re-attempted queue item #1 (gas) first: `sui` CLI, `fullnode.testnet.sui.io` RPC, `github.com` releases, and `crates.io` (API + binary CDN) all return `403` from the sandbox's egress proxy — confirmed as an org policy denial (proxy diagnostics), not a transient failure. Still BLOCKED, re-ranked with that distinction. Moved to item #2: 12 isolated-gadget fixtures (`circuits/bench/attribution/*.circom`) + `scripts/bench/constraint-attribution.sh`, compiled with `circom2` (npm-installable, verified byte-identical to the native circom 2.2.2 build). Every circuit's non-linear total reconstructs exactly from gadget counts (transfer 6470, compliance 6057, withdraw 1465 — zero residual beyond three named boolean/AND checks already in the source). Poseidon calls are 93–94% of non-linear constraints in transfer/compliance, 78% in withdraw; the Merkle tree alone is 75–80%. Reading `circomlib/poseidon.circom` shows why a same-arity Poseidon→Poseidon2 swap can't move that number: R1CS only charges for the S-box's signal×signal multiplications, and every linear/MDS layer (Poseidon2's actual efficiency claim) is already free regardless of which one is used — derived formula `24t + 3·nRoundsP(t)` matches all four measured arities exactly. `BASELINE.md` updated with the attribution table; no circuit or contract code changed. Full writeup: [`2026-08-20-poseidon-constraint-attribution.md`](2026-08-20-poseidon-constraint-attribution.md). |
