# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-04 | `poseidon2-vs-poseidon` | Compiling with circom's full simplification (`--O2`) cuts Veil's R1CS constraints more than swapping Poseidon for Poseidon2's linear layer (t=3, official BN254 params) does under `--O1`. | **KEEP** (`--O2` compile flag) / **REJECT** (Poseidon2 swap) / **BLOCKED** (on-chain gas, unchanged) | Attempted queue item #1 (on-chain gas) again first — still blocked, now with a sharper diagnosis: `fullnode.testnet.sui.io` and `static.crates.io` both hard-`403` at the sandbox network proxy (standing policy, not a retriable tool-approval prompt). Moved to item #2. Built a real Poseidon2 (t=3) circom circuit from official BN254 parameters (fetched from `HorizenLabs/poseidon2`'s reference implementation, verified bit-exact against its own published KAT both in JS and in the circom translation). At Veil's actual default (`--O1`) compile flags it *increases* total constraints +12.2% (580 vs 517) despite 3 fewer S-box constraints; at `--O2` both converge to an identical 240 non-linear constraints — REJECT the swap, not worth the audit/ceremony cost for zero measured benefit, and Veil's dominant Poseidon width (t=5) has no published BN254 Poseidon2 parameters at all. Comparing `--O1` vs `--O2` on Veil's *real* circuits, though, is the night's actual result: 53% fewer constraints uniformly across all three circuits, 8.5–24.2% faster real Node proving time, 2.0–25.1% faster browser proving time, and the existing 108-test suite (including every adversarial/malicious-witness test) passes identically against fresh `--O2` builds. `compile{,-compliance,-withdraw}.sh` now pass `--O2`; `BASELINE.md` updated in place to the new numbers. Full writeup: [`2026-08-04-poseidon2-vs-poseidon.md`](2026-08-04-poseidon2-vs-poseidon.md). |
