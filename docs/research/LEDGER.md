# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-03 | `poseidon-constraint-attribution` | Swapping circomlib `Poseidon` for a Poseidon2-style permutation measurably reduces `transfer.circom`/`compliance.circom` non-linear constraint count. | **REJECT** (hypothesis) — attribution data **KEPT** in `BASELINE.md` | Item #1 (gas) re-attempted first with two genuinely new approaches (arbitrary `github.com` git clone works via this session's proxy rewrite; `cargo`'s own git fetcher needed `net.git-fetch-with-cli=true` to use it) — build got to 4.8GB/1600+ crates compiled but did not finish in budget; JSON-RPC re-tested against 3 different Sui RPC hosts, all a confirmed categorical `403` egress-policy block (not the "tool-approval" issue from last time) — **PARKed**, re-queued with the `git-fetch-with-cli` fix preserved. Fell back to item #2: built `scripts/bench/poseidon-constraint-attribution.sh`, compiling 11 isolated single-gadget probe circuits + the shared `MerkleProof(20)` template, and reconciled them **exactly** (constraint-for-constraint) against fresh full-circuit compiles of all three circuits. Finding: Poseidon2's design change (a cheaper linear/MDS layer) has no R1CS analogue — circomlib's `Mix`/`MixLast`/`MixS` are already pure linear combinations, zero non-linear cost, regardless of matrix. Real finding instead: the depth-20 Merkle path alone is 76–81% of the two dominant circuits' non-linear constraints — redirects queue item #4 to top priority. Full writeup: [`2026-08-03-poseidon-constraint-attribution.md`](2026-08-03-poseidon-constraint-attribution.md). |
