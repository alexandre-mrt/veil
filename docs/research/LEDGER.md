# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-03 | `transfer-constraint-attribution` | The depth-20 Merkle membership proof (`Poseidon(2)`×20) contributes more of `transfer.circom`'s non-linear constraints than the four Poseidon instances named in the circuit's own audit-fix comments combined. | **KEEP** | Re-attempted on-chain gas (queue item #1) first: now **BLOCKED** for a precisely documented reason — GitHub access is scoped to this repo only (`github.com/MystenLabs/sui` returns 403 regardless of tag/endpoint), and direct network egress to any other host (incl. a public Sui fullnode JSON-RPC endpoint, no GitHub involved) is denied by sandbox org policy, confirmed non-retryable. Pivoted to queue item #2 with a measurement instead of a circuit port: built `scripts/bench/constraint-attribution.mjs`, which isolates every circomlib gadget `transfer.circom` uses, compiles each standalone with `circom2` (an npm-published WASM circom compiler — GitHub access to `iden3/circom`, last night's toolchain, is out of scope this session), and reconstructs the total to within 1 of 7,141 constraints against a fresh, verified-matching recompile of the real circuit. Result: 93.1% of non-linear constraints are Poseidon, and 75.1% come specifically from the 20 `Poseidon(2)` Merkle-path calls — not the four instances the queue item's own wording pointed at. Did not attempt a Poseidon2 port itself: no vetted BN254 Poseidon2 circom template is reachable from this session (npm has none; GitHub is out of scope), and hand-deriving round constants/MDS matrix without a way to cross-check them was rejected as unsafe under time pressure. Full writeup: [`2026-09-03-transfer-constraint-attribution.md`](2026-09-03-transfer-constraint-attribution.md). |
