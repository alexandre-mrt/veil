# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-22 | `poseidon2-hash-swap` | Swapping circomlib Poseidon for Poseidon2 at the two hash arities with an audited BN254 parameterization (t=3 Merkle path, t=4 amount/nullifier hashes) reduces R1CS constraints and Groth16 proving time. | **REJECT** (swap) / **KEEP** (a side-finding: adopt `circom --O2`) | Hypothesis falsified: at circom's `--O1` default, the Poseidon2 swap makes total constraints **worse** (+11–14%, un-collapsed linear-constraint bloat from the vendored implementation's per-round components); at `--O2` (full simplification) it's a statistical wash (+0.1–0.2%) at either arity. Built and cross-validated (`circuits/experiments/poseidon2/`, `circuits/vendor/poseidon2/`), 8/8 new tests pass including malicious-witness negative tests, but not adopted. Along the way: discovered Veil's own `compile*.sh` scripts were using circom's `--O1` default, not `--O2` — switching cut total constraints ~53% and Node proving time ~23% (transfer/compliance) for zero semantic change (108/108 existing circuit tests pass identically, same negative-test behavior). Adopted: `compile*.sh` now pass `--O2`, `BASELINE.md`/`README.md`/`docs/threat-model.md`/`docs/SPEC.md` updated with the new constraint counts. On-chain gas remains **BLOCKED**, third attempt — root-caused this time to an egress-policy `403` on both `github.com` release downloads and direct JSON-RPC to the testnet fullnode (git's own HTTPS access to `github.com` *is* permitted, which is a distinction worth knowing for future runs). Full writeup: [`2026-08-22-poseidon2-hash-swap.md`](2026-08-22-poseidon2-hash-swap.md). |
