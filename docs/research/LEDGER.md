# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-15 | `onchain-gas-baseline` | On-chain gas per entry point (blocked 2026-07-22) can be measured with real `gasUsed` by driving a fully local `sui` network instead of needing a public fullnode. | **KEEP** (see 2026-09-15 addendum row below — this finding turned out to duplicate PR #59, 2026-09-11) | Unblocks queue item #1 **as visible from `main`**, though not as visible from the full (unmerged) PR history — see addendum. Downloaded a prebuilt `sui 1.79.0` release binary (GitHub release assets are reachable; public RPC/fullnode hosts across every provider tried are blocked by network policy — confirmed, not a fluke). Ran a single-validator local network (`sui start --with-faucet`, zero external dependency), deployed the real package, drove `deposit_and_register`, `shielded_transfer`, `zk_withdraw`, `compliant_transfer`, `create_compliance_config`, `freeze_pool`/`unfreeze_pool` with real Groth16 proofs, recorded real `gasUsed`. Finding: verification **computation** cost is flat (Sui's cheapest bucket) regardless of circuit size or proof count — gas differences are entirely storage. Also unblocked and ran the Move test suite for the first time visible on `main` (124/124 pass), and fixed two real bugs in `scripts/src/deploy.ts` (wrong digest field; `sui client publish` incompatible with the current CLI's package-management feature). `BASELINE.md` updated in place. Full writeup: [`2026-09-15-onchain-gas-baseline.md`](2026-09-15-onchain-gas-baseline.md). |
| 2026-09-15 | `pr63-ci-triage-addendum` | Diagnostic, discovered while driving PR #63's CI to green, not a new performance hypothesis. | **KEEP** (CI fixes) / flagging a **Critical** unfixed finding | PR #63's CI came back red on 3 of 4 jobs immediately after opening. Investigating turned up the same backlog `ci-backlog-audit` (PR #55, 2026-09-06) already diagnosed: **60+ open, unmerged `research:`/`ci:` PRs (#18-#63+)**, none merged since 2026-07-22, because CI has been red on nearly all of them (corrupted `oven-sh/setup-bun` SHA pin; `storage.googleapis.com` ptau download now 403ing). Both fixes were already independently derived and verified on unmerged branches (PR #55 for the bun pin, PR #62 for the ptau — replace the download with a locally-generated Powers of Tau via `snarkjs powersoftau new/contribute/prepare phase2`, which this session had *also* independently arrived at for its own local `sui` network setup). Ported both into `.github/workflows/ci.yml` on this session's branch rather than re-deriving them a 3rd/4th time. Also discovered, while comparing notes with the backlog: this same night's `onchain-gas-baseline` entry above **duplicates PR #59's 2026-09-11 finding** (same headline result: verification computation gas is flat, storage dominates) — invisible from `main` when tonight's run started, same root cause as every other duplicate in the backlog. More importantly, PR #59 also contains an unfixed **Critical** finding this session had not found on its own: `pool::zk_withdraw` never checks its `recipient` argument against the proof's `recipientHash` public input, letting a relayer or front-runner redirect any pending withdrawal's payout to their own address using someone else's valid proof — independently reconfirmed against current `main` by reading the code directly (`contracts/sources/pool.move:571-632`; the "front-running is prevented" code comment there is false). Documented as `docs/threat-model.md` **RR10** and the STRIDE **E7** entry corrected from "Mitigated" to "NOT mitigated" (neither existed on `main` before tonight — RR10 existed only on PR #59's unmerged branch). Promoted to #1 in `EXPERIMENTS.md`, ahead of every performance item. Not fixed in this PR — a real fix needs a decided address-to-field-element encoding, an on-chain check, a soundness argument, and a negative test, none of which belong in a CI-triage/gas-measurement PR. |
