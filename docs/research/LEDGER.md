# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-09 | `poseidon2-linear-layer` | Swapping Poseidon's dense full-round MDS matmul for a Poseidon2-style sparse linear layer (same round constants/S-box/round count, only the linear-mix term density varies) reduces R1CS constraint count and Groth16 proving time for Veil's two real arities (t=3, t=5). | **REJECT** | On-chain gas (queue #1) re-attempted first, still **BLOCKED** — now precisely characterized: `fullnode.testnet.sui.io` is denied by sandbox network policy (403), and `MystenLabs/sui` release binaries are unreachable because this session's GitHub scope covers only `alexandre-mrt/veil`; no `sui` CLI crate exists on crates.io beyond a name reservation. Pivoted to queue #2. Measured: R1CS constraint count is **exactly identical** between dense and cheap linear layers at both t=3 (519) and t=5 (740) — circom's default `--O1` linear-substitution simplification absorbs any constant linear combination regardless of term count, so Poseidon2's core mechanism (fewer native field multiplications) has nothing to save in R1CS/Groth16. Proving-time delta was sub-standard-deviation and non-reproducible across two independent 30-run trials. Negative test (12/12 checks) confirmed both variants' constraints genuinely bind their outputs. Full production test suite unaffected (43+30+35 circuit tests, 109 converter, 67 compliance-utils, 19 frontend, 6×500 fuzz — all pass; Move suite still un-runnable, same blocker as 2026-07-22). `BASELINE.md` unchanged (no production circuit touched — purely additive `circuits/bench/`). Full writeup: [`2026-08-09-poseidon2-linear-layer.md`](2026-08-09-poseidon2-linear-layer.md). |
