# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-06 | `poseidon-constraint-decomposition` | Every circuit's non-linear constraint total can be decomposed into an exact per-gadget attribution (N instances of Poseidon at each arity, the depth-20 Merkle proof, range checks) that sums to the whole-circuit total, replacing the guess in `EXPERIMENTS.md` about which gadget dominates. | **KEEP** | Attempted queue item #1 (on-chain gas) first, per its own instruction to spend early effort unblocking it — confirmed **BLOCKED** a third time, now with definitive evidence: `github.com`, `crates.io`, and `fullnode.testnet.sui.io` all return `403` policy denials from this session's egress proxy (not a toolchain gap this sandbox can fix; re-ranked with a note that it needs an infra decision, not another attempt). Pivoted to queue item #2's stated alternative: built 11 isolated single-gadget circuits under `circuits/bench/` + `scripts/bench/gadget-constraints.sh`, measured each with `circom2` (npm-distributed WASM compiler, since GitHub/cargo were blocked), and reconciled the sums exactly against `BASELINE.md`'s existing totals. Finding: the 20-level `MerkleProof` — not the four domain-tagged top-level Poseidon calls — dominates `transfer.circom` (76%) and `compliance.circom` (81%); `withdraw.circom` (no Merkle proof) is the one circuit where the top-level calls really do dominate (78%). No production circuit touched. Re-ran all 108 real-Groth16 circuit tests + 109 converter tests + 19 frontend tests from a clean rebuild (`circom2` toolchain) — all pass. `scripts/test-compliance-utils.ts`'s depth-20 real-Poseidon test did not complete within budget (killed, not counted either way — same gap as 2026-07-22). Full writeup: [`2026-08-06-poseidon-constraint-decomposition.md`](2026-08-06-poseidon-constraint-decomposition.md). |
