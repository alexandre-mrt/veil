# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-27 | `poseidon2-hash-swap` | Swapping Poseidon for a domain-separated Poseidon2 sponge (`@taceo/circom-lib`) in `transfer`/`withdraw`/`compliance` reduces Groth16 proving time, moving the number every transfer and withdrawal pays. | **KEEP** (research artifacts — variant circuits + bench harness merged, `BASELINE.md` updated; production circuits/VKs/frontend **not** swapped, see report) | Built 8 isolated microbenchmark circuits (every Poseidon arity Veil calls) + 3 full production-shaped `_v2` variants under `circuits/bench/poseidon2/`. Cross-checked a from-scratch JS Poseidon2-sponge implementation against the compiled circuit's real witness (4/4 exact match) before trusting it to build full-circuit witnesses. Result is more nuanced than the hypothesis assumed: total R1CS constraints go **up** ~10-12% for every circuit (implementation-level linear-layer constraint bloat in the published templates, not fundamental), but measured Groth16 proving time still goes **down** — `transfer` -5.4%, `withdraw` -13.3% — because none of the three circuits cross a power-of-two constraint boundary and non-linear (witness-gen) constraints drop in every shape. `compliance` is a wash (+0.1%, within noise) because its credential-leaf hash needs Poseidon2 state size t=5, which `@taceo/circom-lib` doesn't publish parameters for — partial swap only. Negative test (forged commitment, forged nullifier against `withdraw_v2`) confirms no under-constrained signal was introduced. Zero leakage delta: public-input signal sets are byte-identical to the production circuits. Also re-confirmed queue item #1 (on-chain gas) as structurally BLOCKED this session (RPC + GitHub releases + crates.io all denied/unavailable), but discovered `git clone` of `MystenLabs/sui` *does* work even though release downloads don't — a from-source build is reachable but needs a dedicated night, re-noted in `EXPERIMENTS.md`. Full writeup: [`2026-08-27-poseidon2-hash-swap.md`](2026-08-27-poseidon2-hash-swap.md). |
