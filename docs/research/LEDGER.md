# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-26 | `poseidon2-arity-gap` | Swapping Poseidon(n) for Poseidon2 reduces R1CS constraints and Groth16 proving time at Veil's actual call arities (n=2,3,4,5). | **REJECT** | Re-checked on-chain gas (queue #1) first — still blocked, now confirmed as org egress policy (403 on `github.com/MystenLabs/sui/releases` and `fullnode.testnet.sui.io`), not a toolchain gap; did not retry further per proxy policy. Moved to queue #2. Built 8 isolated single-hash-call circuits (circomlib `Poseidon(n)` vs `@taceo/circom-lib`'s `Poseidon2Sponge(n,t)`) plus a domain-separation check, all real Groth16 setups + 10-run proving-time benchmarks. Finding: `@taceo/circom-lib`'s Poseidon2 only ships round constants for t ∈ {2,3,4,8,12,16} — no t=5/6 — so Veil's two heaviest arities (n=4, n=5) must over-provision to t=8, costing +927/+828 constraints per call (vs +63/+247 for n=2/n=3, which do have exact-width support and measurably prove faster). A computed (not measured) projection onto the real production circuits shows `transfer.circom` and `withdraw.circom` would likely cross a Groth16 QAP domain-size doubling (16,384→32,768 and 4,096→8,192) — working against the goal. `compliance.circom` stays under threshold but gains nothing. Full writeup: [`2026-08-26-poseidon2-arity-gap.md`](2026-08-26-poseidon2-arity-gap.md). |
