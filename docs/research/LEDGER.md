# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-13 | `poseidon2-arity-gap` | Swapping circomlib's Poseidon for Poseidon2 (`@taceo/circom-lib`) at Veil's supported arities (t=3, t=4) reduces R1CS constraint count per hash call by a double-digit percentage. | **REJECT** | Measured, not estimated: at default optimization (what `compile*.sh` ships) Poseidon2 is 12–41% *more* total R1CS constraints at both arities; at `--O2` the gap closes to parity (t=3) or a 1.1% regression (t=4). Non-linear constraint count is flat either way. Correctness cross-checked against an independent JS reference (`@taceo/poseidon2`, 13/13 tests pass, including negative tests that a forged public hash is rejected). Separately, 6 of Veil's 10 Poseidon call sites (t=5, t=6 — the commitment/nullifier hashes, the highest-value targets) have no published Poseidon2 parameter set at all in the libraries checked, so a full migration is structurally blocked regardless of tonight's result. No production circuit was modified — all additive under `circuits/bench/poseidon2/` and `scripts/bench/`. On-chain gas (queue item 1) remains BLOCKED, unattempted tonight (same network-policy denial as 2026-07-22, confirmed again, not retried per policy). Full writeup: [`2026-09-13-poseidon2-arity-gap.md`](2026-09-13-poseidon2-arity-gap.md). |
