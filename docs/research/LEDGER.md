# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-21 | `poseidon2-hash` | Swapping Poseidon → Poseidon2 (queue item #2) drops non-linear constraints ~10% per circuit and moves Node proving time down, without changing the public statement or leakage profile. | **REJECT** | Built three shadow circuits (`transfer2`/`compliance2`/`withdraw2.circom`, not wired into the deployed protocol) using `@taceo/circom-lib`'s Poseidon2, cross-validated against an independent JS re-implementation (bit-for-bit match at T=2 and T=4), then measured for real. Non-linear constraints *did* drop 9-10% as hypothesized, but linear constraints grew more (+4.7% to +28.2%) and **Node proving time got slower for all three circuits by a similar margin** (transfer +13.8%, compliance +14.5%, withdraw +12.0%, mean of 10 runs) — the hypothesis's own number moved the wrong way, and roughly uniformly regardless of how much Merkle-tree hashing each circuit does. 7/7 negative tests (forged domain tags, tampered Merkle path, non-boolean path selector, forged credential leaf) correctly rejected — including one added after catching a real drafting bug (a missing `pathIndices` boolean check) while writing this report, fixed before any numbers were finalized. Item #1 (on-chain gas) re-attempted first and reconfirmed **BLOCKED**: this session's proxy denies `github.com`/`crates.io`/the Sui fullnode with a policy-level 403, not a missed attempt — see the report. Full writeup: [`2026-08-21-poseidon2-hash.md`](2026-08-21-poseidon2-hash.md). |
