# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-29 | `poseidon2-primitive-delta` | Swapping Veil's Poseidon(2/3/4/5) calls for Poseidon2 at matching state widths reduces R1CS constraints and proving time, under the compile flags the production circuits actually use. | **KEEP** (measurement; substantive finding is a soft REJECT of a Poseidon2 migration) | Re-attempted queue item 1 (on-chain gas) first, per its own note — still **BLOCKED**, now for a confirmed org-network-policy reason (`fullnode.testnet.sui.io` and `github.com` both `403` at the egress proxy; no real `sui` crate on crates.io either), not a tooling gap — see the report for the exact evidence. Pivoted to queue item 2: built standalone (not protocol-wired) Poseidon vs `@taceo/circom-lib` Poseidon2 benchmark circuits at Veil's real arities, cross-validated Poseidon2(t=4)'s output against two independent JS implementations before citing any number, added a negative test. Result: at Veil's arities and its actual (non-`--O2`) compile flags, Poseidon2 costs **more** constraints (+12% to +126%, worst for the dominant `Poseidon(4)` call since Poseidon2 has no t=5 and must pad to t=8), and proving time doesn't clearly favor either at this scale. Full writeup: [`2026-08-29-poseidon2-primitive-delta.md`](2026-08-29-poseidon2-primitive-delta.md). |
