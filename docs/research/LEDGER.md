# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-30 | `poseidon-constraint-attribution` | The exact non-linear-constraint contribution of every named gadget (Poseidon per arity, range checks, the Merkle template) can be measured by compiling each alone, bounding what a future Poseidon2 swap could ever save. | **KEEP** | Re-attempted item #1 (on-chain gas) first: confirmed it is now **structurally blocked**, not a toolchain gap — `sui` CLI is gated by this session's GitHub access being scoped to `alexandre-mrt/veil` only (`add_repo` required for `MystenLabs/sui`), and direct JSON-RPC to six different Sui fullnode providers all get an identical `connect_rejected`/403 network-policy denial. Needs human action; re-ranked with this noted so future nights don't re-spend a night on it. Pivoted to item #2's alternate framing: built `scripts/bench/constraint-breakdown.mjs`, isolating every gadget in all three circuits via `circom2` (an npm-distributed WASM circom 2.2.3 build — a new, GitHub-independent reproduction path for this environment, also folded into `BASELINE.md`). Result: `transfer.circom` is 93.1% Poseidon, `compliance.circom` 94.3%, `withdraw.circom` 78.0% — reconciliation against fresh full-circuit compiles is exact (0–3 constraint residual, fully explained). The Poseidon2 **port** itself is **PARK**ed — no verified `.circom` Poseidon2 gadget is reachable from this session (same GitHub-scoping root cause as item #1); writing one from memory was rejected as unverified crypto. Full test suite green except Move (same pre-existing `sui`-CLI gap). Full writeup: [`2026-08-30-poseidon-constraint-attribution.md`](2026-08-30-poseidon-constraint-attribution.md). |
