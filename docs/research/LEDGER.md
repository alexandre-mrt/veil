# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-31 | `merkle-zero-hash-pruning` | Zero-hash-pruning the two off-chain depth-20 Merkle builders (`compliance-utils.ts`, `frontend/lib/merkle-tree.ts`) turns build/proof cost from `O(2^depth)` into `O(n + depth)` Poseidon calls, bit-identical output, and the delta at `n=1` (today's real usage) is the number that matters. | **KEEP** | Queue item 1 (on-chain gas) re-confirmed **BLOCKED** by a second, independent method (JSON-RPC to testnet fullnode: `403`; `cargo`/crates.io content for a from-source build: `403` — only index metadata and `api.github.com` reachable, git-protocol clones work but can't fetch dependency content). Took the off-chain half of item 4 instead. Measured: naive `n=1` depth-20 build = 101,358 ms (constant regardless of n, by construction); pruned build = 43.8 ms at n=1 up to 108,763 ms at full occupancy (n=2^20, correctly no speedup — no padding left to skip). Also ran depth-24 (~16.8M capacity, inside item 4's 10^5-10^7 range) to show the technique removes the "off-chain rebuild is impractical at greater depth" objection, though raising the on-chain depth itself is still an unstarted circuit + trusted-setup change. `BASELINE.md` updated. Also confirmed, as a side effect of running the full suite, that `EXPERIMENTS.md` item 12 (chained `npm test` hang) is already fixed on `main` (PRs #16/#17, pre-dating tonight). Full writeup: [`2026-08-31-merkle-zero-hash-pruning.md`](2026-08-31-merkle-zero-hash-pruning.md). |
