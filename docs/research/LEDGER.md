# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-25 | `poseidon-merkle-constraint-isolation` | More than half of `transfer.circom`'s and `compliance.circom`'s non-linear constraints come from the single depth-20 `MerkleProof` component, not the top-level identity/nullifier Poseidon calls. | **KEEP** | Queue item #2's alternate form (isolate exact per-Poseidon-instance constraint contribution) rather than a full Poseidon2 swap — no verifiable Poseidon2 circom reference is reachable this session (see notes). Built `scripts/bench/poseidon-isolation.mjs` + 5 standalone one-component circuits; confirmed `MerkleProof(20)` alone is 76-81% of `transfer`/`compliance`'s non-linear constraints (4,920 of 6,470 / 6,057), vs. 94.0%/78.0%/95.3% Poseidon share overall for transfer/withdraw/compliance. Derived a per-Merkle-level cost of 246 non-linear constraints, feeding RR5's anonymity-set-depth trade-off. Also found this session's egress policy is markedly tighter than 2026-07-22's: `github.com`, `static.crates.io`, `storage.googleapis.com` (the Hermez ptau URL), and every public Sui RPC/explorer host tried are now `403`-blocked — on-chain gas (queue item #1) is BLOCKED for a second consecutive night, and circuit tests ran HASH-ONLY (not real Groth16) because the ptau download that worked last time no longer does. Worked around the compiler half of that with `circom2` (npm, WASM circom 2.2.3), verified byte-identical to last night's native-binary constraint counts. Full writeup: [`2026-09-25-poseidon-merkle-constraint-isolation.md`](2026-09-25-poseidon-merkle-constraint-isolation.md). |
