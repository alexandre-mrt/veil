# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-01 | `poseidon-constraint-decomposition` | Every non-linear constraint in all three circuits can be attributed to a named gadget instantiation (Poseidon arity, Num2Bits/comparator width, or explicit top-level glue) with zero unexplained residual, closing the 2026-07-22 report's open question on Poseidon vs. range-check cost share. | **KEEP** | Built `scripts/bench/poseidon-decompose.mjs` + 12 isolated probe circuits (`circuits/bench-probes/`); reconstruction matches `BASELINE.md` exactly (0 residual, both non-linear and linear) for all three circuits. Headline: the four *named* Poseidon calls are a minority of the cost — 76–81% of `transfer.circom`/`compliance.circom`'s non-linear constraints are the 20 `Poseidon(2)` calls hidden inside `MerkleProof(20)`, uncounted by the existing source comments. `BASELINE.md` gained a new breakdown section (no existing row superseded). Full test suite green except Move contracts (still no `sui` CLI). Also spent early session time re-attempting queue item #1 (gas): confirmed the JSON-RPC path is an explicit network-policy 403 (not the prior ambiguous denial), `cargo install sui` resolves to a nameless placeholder crate, and `circom` isn't on crates.io at all — but plain `git clone` to arbitrary GitHub repos now works (unlike `api.github.com`/`codeload`), reopening a from-source `sui` build as a real option for a future, budgeted night. Full writeup: [`2026-08-01-poseidon-constraint-decomposition.md`](2026-08-01-poseidon-constraint-decomposition.md). |
