# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-19 | `poseidon2-merkle-swap` | Swapping circomlib's `Poseidon(2)` for Poseidon2 (official BN254 params, `t=3`) in the depth-20 Merkle-hash path (`transfer.circom`, `compliance.circom`) and `withdraw.circom`'s `recipientHash` measurably cuts R1CS constraints and Groth16 proving time. | **REJECT** (for production adoption — validated and kept as reference code) | Re-checked on-chain gas (queue #1) first: still **BLOCKED**, now confirmed as a hard org-level network-proxy policy denial (`fullnode.testnet.sui.io` → `403`), not a retryable per-call approval prompt; GitHub access is also scoped to this repo only, closing the "fetch a prebuilt `sui` binary" path too. Moved to queue #2. Built `templates/poseidon2_bn254_t3.circom` from the *published* Horizen Labs BN254 parameters (not self-derived), validated against the reference implementation's own KAT at both the JS and circuit level before use. Real measured deltas: constraints −0.3% (`transfer`/`compliance`, 20 Merkle-hash calls each), −0.07% (`withdraw`, 1 call) — much smaller than the paper's headline numbers because circomlib's existing `Poseidon(2)` is already well-optimized at `t=3`. Real Groth16 proving time: −4.0% (`transfer`), −2.2% (`compliance`), −5.6% (`withdraw`), reproduced across three independent benchmark runs (one accidentally CPU-contended — see report for the measurement mistake and how it was caught). 11/11 new tests pass (real Groth16 full proofs), including negative tests proving a mismatched hash family (Poseidon vs Poseidon2 on the same Merkle path) is rejected, not silently accepted. Verdict is REJECT for production because the real win (2–6% proving time) doesn't clear the real migration cost (VK-timelock governance, a breaking Merkle-tree hash-family migration for already-inserted commitments, added audit surface for a 2023-published primitive on a pre-audit protocol) — not because the swap doesn't work. Production circuits, `BASELINE.md`, and `docs/threat-model.md` are unchanged; the validated circuit/test/bench code is merged as reference material. Full writeup: [`2026-09-19-poseidon2-merkle-swap.md`](2026-09-19-poseidon2-merkle-swap.md). |
