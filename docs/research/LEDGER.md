# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-09-26 | `poseidon2-merkle-path` | Swapping the depth-20 Merkle path's Poseidon(2) sponge hash for Poseidon2 compression (t=2) reduces `transfer.circom`'s non-linear constraint count by a measurable double-digit percentage. | **KEEP** | Queue item #1 (on-chain gas) re-attempted first and re-BLOCKED for the same class of reason (GitHub releases + crates.io still return 403 through the proxy; not retried past that, per policy) — moved to item #2 instead. Built `transfer_poseidon2.circom` (research variant, C0 only swapped) + `templates/merkle_proof_poseidon2.circom` using `@taceo/circom-lib`'s Poseidon2, verified against the `@taceo/poseidon2` JS reference (11/11 correctness + negative tests, `circuits/test/poseidon2.test.mjs`). Measured: -540 non-linear constraints (-8.3%), -660 total (-4.85%) on the full circuit; exact cause isolated to full-round S-box count (t=3→t=2) and one fewer partial round, not a general Poseidon2-is-faster effect. Powers-of-Tau had to be generated locally (both known mirror URLs now 403/AccessDenied — external, not this session's proxy). Proving-time benchmark and the full repo test suite: see writeup for what finished before the run ended. Not wired into production (`pool.move` unchanged). Full writeup: [`2026-09-26-poseidon2-merkle-path.md`](2026-09-26-poseidon2-merkle-path.md). |
