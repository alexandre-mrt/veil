# Research ledger

Append-only. One row per night. Never edit or delete a past row — if an experiment gets rerun,
add a new row and say why in the notes; don't overwrite history.

| Date | Slug | Hypothesis | Verdict | Notes |
|---|---|---|---|---|
| 2026-07-22 | `baseline-measurement` | Every core Veil performance number (constraints, zkey/vk/proof size, Node + browser proving time) can be measured directly on one machine with real command output, not estimated. | **KEEP** (partial) | First run of this loop — bootstrapped `docs/research/` itself (this ledger, the queue, `NIGHTLY_PROMPT.md`) alongside the experiment. `BASELINE.md` created. All three circuits measured end-to-end (constraints, zkey/vk size, Node + browser proving time via a real headless-Chromium run). On-chain gas per entry point is **BLOCKED**: no `sui` CLI available or buildable within budget, and a fallback JSON-RPC read against the deployed testnet package was denied by the sandbox's tool-approval layer mid-session (not retried, per policy). Full writeup: [`2026-07-22-baseline-measurement.md`](2026-07-22-baseline-measurement.md). |
| 2026-08-24 | `poseidon2-merkle-hasher` | Swapping circomlib's `Poseidon(2)` for Poseidon2(t=3) in the depth-20 Merkle-path hasher measurably reduces constraint count and proving time. | **REJECT** | *Backfilled 2026-09-12 from PR #42 (unmerged until then — see the 2026-09-12 row) — copied from that branch's own ledger row, not re-verified line-by-line.* Non-linear constraints drop slightly (~1%) as predicted, but the `@taceo/circom-lib`-based template's linear-layer encoding (named-intermediate-signal MDS multiplies) costs far more in linear R1CS constraints: transfer/compliance +1,260 constraints (+9.3%/+9.9%), withdraw +63 (+2.1%). Real proving-time regression measured in both Node (+14–19%) and headless Chromium (+6–11%). Cross-checked against `@taceo/poseidon2`'s JS implementation first. 109/109 circuit tests pass (real Groth16), including a negative test proving an old-hasher root is rejected. `main` unchanged. Full writeup (on that branch): `2026-08-24-poseidon2-merkle-hasher.md`. |
| 2026-09-06 | `ci-backlog-audit` | Diagnostic: why do queue items #1/#2 still show unsettled after ~40 nights that each claim to have run them? | **KEEP** (CI fix, diagnosed) / **PARK** (backlog cleanup, needs a maintainer) | *Backfilled 2026-09-12 from PR #55 (still unmerged as of that date — its own fix didn't land until the 2026-09-12 row below re-applied it).* Found 35 open unmerged research PRs (#18–#54) — `main`'s ledger/queue frozen at 2026-07-22 the whole time, so nights kept re-deriving the same conclusions blind to each other (6+ independent Poseidon2 REJECTs, 3+ independent gas-BLOCKED findings by that point). Root cause: CI red on every PR — `oven-sh/setup-bun` pinned to a SHA that doesn't exist (fails in ~2s before any test runs; a fix for this exact bug had *already* been diagnosed once on 2026-08-31 and also never merged), plus separate `403`s on the `sui`-CLI and `pot15.ptau` downloads. Diagnosed a concrete fix for each; only re-applied the bun-pin fix in that PR (scope discipline). That PR's fix itself then sat unmerged for another 6 nights — see 2026-09-12. Full writeup (on that branch): `2026-09-06-ci-backlog-audit.md`. |
| 2026-09-10 | `poseidon-constraint-breakdown` | Isolated per-gadget R1CS constraint costs reconstruct each circuit's measured total exactly. | **KEEP** | *Backfilled 2026-09-12 from PR #58 (unmerged until then).* Re-scoped from "swap to Poseidon2" (already independently rejected many times by that point) to measuring exactly where every constraint comes from. Finding: the 20-deep Merkle path (`Poseidon(2)` × 20), not the "four Poseidon instances" the README/2026-07-22 baseline emphasized, dominates — 76.0% of `transfer.circom`, 81.2% of `compliance.circom` (both `--O1`-era numbers; see 2026-09-12 for the post-`--O2` picture). `withdraw.circom` (no Merkle path) is the one circuit where the four-Poseidon framing was already correct (78%). Full writeup (on that branch): `2026-09-10-poseidon-constraint-breakdown.md`. |
| 2026-09-11 | `onchain-gas-baseline` | Every Veil entry point's real gas cost can be measured from `effects.gasUsed` on a real running Sui network, not estimated. | **KEEP** | *Backfilled 2026-09-12 from PR #59 (unmerged until then).* Unblocked queue item #1 by downloading a real prebuilt `sui` 1.79.0 CLI from GitHub releases and measuring gas on a **local** Sui network (local gas equals real-network gas for identical bytecode/inputs). Measured all 10 entry points including both real-proof calls. **Headline finding:** computation gas is flat (minimum bucket) regardless of circuit size — Groth16 verification is 3 pairing checks either way; storage/dynamic-field churn on the shared `Pool` object dominates instead. Ran the full test suite green for the first time (124 Move + 108 circuit real-proof + 109 converter + 67 compliance-utils + 19 frontend + fuzz). **Also found and documented (not fixed): `zk_withdraw` never checks its `recipient` argument against the proof's `recipientHash`** — a real fund-theft path via front-running/relayer substitution. Filed as `docs/threat-model.md` RR10 (Critical) and promoted to top of the queue — see the 2026-09-12 row and RR10 itself (added to `threat-model.md` that same night, since this branch hadn't merged it yet). Full writeup (on that branch): `2026-09-11-onchain-gas-baseline.md`. |
| 2026-09-12 | `ci-backlog-and-O2-optimization` | (Process night, not a single falsifiable hypothesis.) Why has nothing merged since 2026-07-22, and is there a real, safe, unmeasured optimization sitting in the unmerged backlog? | **KEEP** (CI fix + `--O2` adoption) / **PARK** (full backlog cleanup, needs a maintainer) | Re-verified and actually landed PR #55's `oven-sh/setup-bun` CI fix (confirmed the replacement SHA against the live `oven-sh/setup-bun` repo directly, not trusted secondhand). While auditing, found every `circom` invocation in this repo (`compile*.sh`, `ceremony.sh`, CI) was using circom's **`--O1` default**, not `--O2` (full constraint simplification) — a fix independently found twice before (PRs #24, #40, both unmerged) and re-verified here: **-53.1%/-53.1%/-52.9%** R1CS constraints across `transfer`/`compliance`/`withdraw`, functionally verified equivalent (identical accept/reject behavior, same failure line, on both a valid and a deliberately malformed witness). Applied to all three compile scripts, the ceremony script's WASM fallback, and CI; `BASELINE.md` constraint counts updated (proving time/zkey/vk explicitly marked pending re-measurement — this session's own Groth16 setup was too slow to redo, see report). Rewrote `LEDGER.md` (this file, backfilling the four rows above from their unmerged source PRs) and `EXPERIMENTS.md` (added item 0: check the backlog before re-running anything) to reflect the real state of ~45 open PRs instead of the frozen 2026-07-22 snapshot. Independently reproduced RR10 (see 2026-09-11 row) against current `main` and added it to `docs/threat-model.md` directly, since PR #59 hadn't merged. Also ran an independent Poseidon2 Merkle-hasher cross-validation before discovering the backlog (kept as `2026-09-09-poseidon2-merkle-hasher.md` — its **-0.29%/-0.31%** result directly contradicts PR #42's 2026-08-24 **+9.3%/+9.9%** finding for nominally the same swap; flagged as an open question, not resolved). Did not merge or close any of the other PRs — flagged the backlog size and RR10's severity directly to the repo owner instead. Full writeup: [`2026-09-12-ci-backlog-and-O2-optimization.md`](2026-09-12-ci-backlog-and-O2-optimization.md), [`2026-09-09-poseidon2-merkle-hasher.md`](2026-09-09-poseidon2-merkle-hasher.md). |
